{
  "name": "twilio-video-handson-2020",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "zip assets/webrtc-handson-2020 assets/video.* && twilio-run --env"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "8.10.0"
  }
}
